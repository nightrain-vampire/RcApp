<view class="main">
  <loading wx-if="{{loading}}" />
  
  <view class="recommend-bg">
    <view class="tag-area">
      <view class="page-icon">
        <image src="../../static/peo.png" style="width: 100%; height: 100%;"></image>
      </view>
      <view class="title-area">
        <view class="page-title">提名-详情部分</view>
      </view>
    </view>

    <view class="name-area">
      <view class="name-box">
        <view class="name-title">提名名称:</view>
        <input placeholder="(团体/个人的名称)" bindinput="nameInput" class="name-input"></input>
      </view>
    </view>

    <view class="reason-area">
      <view class="reason-box">
        <view class="reason-title">提名人理由:</view>
        <textarea bindinput="reasonBlur" class="reason-input" />
      </view>
    </view>

    <view class="photo_upload-area">
      <form class="photo_upload_bg_bimg" report-submit="true" onsubmit="uploadImgs">
        <view class="photo-title">
          <view class="solid-bottom text-sl" bindtap="ChooseImage" wx:if="{{imgList.length<imgMaxNumber}}"><text class="cuIcon-picfill"></text></view>
          <view class="photo-title2" wx:if="{{imgList.length == 0}}">照片上传</view>
          <view class="display" wx:if="{{imgList.length > 0}}">
            <view class="grid col-4 grid-square flex-sub">
              <view class="bg-img" wx:for="{{imgList}}" wx:key="index" bindtap="ViewImage" data-url="{{imgList[index]}}">
                <image src='{{imgList[index]}}' mode='aspectFill'></image>
                <view class="cu-tag bg-red" catchtap="DelImg" data-index="{{index}}">
                  <text class="cuIcon-close"></text>
                </view>
              </view>
            </view>
          </view>
        </view>
        <view class="photo-title3">限制数量为4，请上传大小在{{picsize}}以内的照片或图片</view>
      </form>
    </view>

    <view class="info-area">
      <view class="info-box">
        <view class="reason-title">提名人信息:</view>
        <textarea bindinput="bindTextAreaBlur" class="info-input" />
      </view>
    </view>

    <view class="button-area">
      <view class="sub-btn">
        <image src="../../static/recom.png" mode="aspectFit" class="button_bimg" bindtap="submit"></image>
      </view>
    </view>

  </view>
</view>